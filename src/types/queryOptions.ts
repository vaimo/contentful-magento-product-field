export type QueryOptions = {
    headers: {
        Accept: string,
        Authorization: string,
        'Content-Type': string,
        credentials: string
    },
    method: string,
    body?: string
}